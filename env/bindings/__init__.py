# C++ pybind11 bindings for accelerated Tetris environment.
# The C++ module `tetris_core` is built via CMake and must be compiled before import.
# See env/bindings/CMakeLists.txt for build instructions.
